HOW TO USE:

git clone <'the name of this repo'>

npm install - to install all the dependencies.

npm run dev - to run in the development mode.


ThreeJS Library and a build tool ViteJS were used in this project.